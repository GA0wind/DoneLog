<script setup lang="uts">
	import { EventManager } from '@/utils/eventManager.uts'
	import { LabelManager } from '@/utils/labelManager.uts'
	import { AccountManager } from '@/utils/accountManager.uts'
	import { consumePendingWidgetAction, saveWidgetSnapshot } from '@/uni_modules/donelogWidget/utssdk'

	// #ifdef APP-ANDROID || APP-HARMONY
	let firstBackTime = 0
	// #endif

	onLaunch(() => {
		console.log('App Launch')
		// 初始化ID
		EventManager.initIds()
		LabelManager.initIds()
		AccountManager.initIds()
		saveTodayWidgetSnapshot()
	})

	onAppShow(() => {
		console.log('App Show')
		saveTodayWidgetSnapshot()
		handlePendingWidgetAction()
	})

	onAppHide(() => {
		console.log('App Hide')
	})

	// #ifdef APP-ANDROID || APP-HARMONY
	onLastPageBackPress(() => {
		console.log('App LastPageBackPress')
		if (firstBackTime == 0) {
			uni.showToast({
				title: '再按一次退出应用',
				position: 'bottom',
			})
			firstBackTime = Date.now()
			setTimeout(() => {
				firstBackTime = 0
			}, 2000)
		} else if (Date.now() - firstBackTime < 2000) {
			firstBackTime = Date.now()
			uni.exit()
		}
	})

	onExit(() => {
		console.log('App Exit')
	})
	// #endif

	const handlePendingWidgetAction = () => {
		const payload = consumePendingWidgetAction()
		if (!payload) {
			return
		}

		try {
			const action = JSON.parse(payload)
			if (action && action.type === 'open-complete' && action.eventId) {
				const event = EventManager.getEventById(action.eventId)
				if (!event) {
					return
				}
				if (event.nextDueDate > 0) {
					uni.navigateTo({
						url: `/pages/event/complete?id=${action.eventId}&entry=widget`
					})
					return
				}
				const success = EventManager.markEventComplete(action.eventId)
				if (!success) {
					return
				}
				if (event.intervalDays < 0) {
					EventManager.terminateEvent(action.eventId)
				}
				saveTodayWidgetSnapshot()
				uni.showToast({
					title: '已完成',
					icon: 'success'
				})
			}
		} catch (e) {
			console.error('处理桌面组件动作失败', e)
		}
	}

	const saveTodayWidgetSnapshot = () => {
		const now = new Date()
		const dayStart = new Date(now.getFullYear(), now.getMonth(), now.getDate()).getTime()
		saveWidgetSnapshot(JSON.stringify({
			items: EventManager.getWidgetSnapshot(),
			overview: AccountManager.getOverviewByDate(dayStart)
		}))
	}
</script>

<style>
	/* 每个页面公共css */
	.uni-row {
		flex-direction: row;
	}

	.uni-column {
		flex-direction: column;
	}

	page {
		background-color: #f4efe7;
		color: #24323a;
	}
</style>
